# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Karthik_TechEvaluationTask' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

     pod 'Alamofire', '~> 4.0'
	pod 'ReachabilitySwift', '~> 3'
        pod 'Kingfisher', '~> 4.0'
        pod 'PKHUD', '~> 5.0'

  # Pods for Karthik_TechEvaluationTask

  target 'Karthik_TechEvaluationTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Karthik_TechEvaluationTaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
